# Cross-Module Contracts

> Generated by Phase 0 P0.2. LLM semantic supplement on module-dep-graph.json skeleton.

## Summary

- **Modules detected**: 1 (`app`)
- **Cross-module source edges**: 0
- **Shared data models**: 0
- **Seam signatures**: 0

## Dependency Context

The `app` module declares gradle dependencies on: `basic`, `bdConvert`, `common`, `docviewer`, `event-track`, `network`, `pay`.

However, `extract_module_deps.py` found **zero cross-module source-level references** (import/call edges). These dependencies are consumed at the binary/AAR level, not via direct source imports in the scanned source tree.

## Impact on Phase A-C

Since there are no cross-module source edges:
- **Phase A-C runs once** for the `app` module (single shard).
- No seam contracts need to be injected into shard generation.
- **C4.6d seam audit is skipped** (0 edges to verify).

## Gradle Dependency Manifest (for reference)

| Module | Dependency Type | Notes |
|--------|----------------|-------|
| basic | implementation | Base utilities |
| bdConvert | implementation | Convert/transform |
| common | implementation | Shared common code |
| docviewer | implementation | Document viewer |
| event-track | implementation | Event tracking |
| network | implementation | Network layer |
| pay | implementation | Payment |
